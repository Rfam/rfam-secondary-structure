{
  "name": "rfam-secondary-structures",
  "version": "1.0.0",
  "description": "Reusable component for Rfam secondary structures",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build --mode standalone && gh-pages -d dist/standalone",
    "build:lib": "vite build --mode library",
    "build:standalone": "vite build --mode standalone"
  },
  "dependencies": {
    "prop-types": "^15.8.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "svg-pan-zoom": "^3.6.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.8"
  }
}
